<div>
  <%= flash[:notice] %>
</div>

<div class="book-wrap">
<% @books.each do |book| %>
  <div class="book-comment">
    <span class="book-body"><%= book.感想%></span>
    <span class="book-title"><%=link_to book.本のタイトル,book_path(book.id) %></span>
  </div>
<%end%>
</div>
